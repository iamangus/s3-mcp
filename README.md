# OpenDev

## OpenDev CI

OpenDev CI runs for pull requests when they are opened, reopened, or updated with new commits. It uses the shared OpenDev CI workflow with the repository's automatic profile to validate proposed changes.
